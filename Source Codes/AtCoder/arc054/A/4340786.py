def a_moving_walkway(L, X, Y, S, D):
    """
    ??L????????X????????????????Y????????
    ???????????0???????????????????
    ??S??????D????????????????????????
    """

    distance_start_to_goal = D - S if S <= D else D + (L - S)
    if X < Y:
        # ??????????????????????????
        ans = min(distance_start_to_goal / (X + Y),
                  (L - distance_start_to_goal) / (Y - X))
    else:
        # ????????????
        ans = distance_start_to_goal / (X + Y)
    ans = round(ans, 9)
    return ans

L, X, Y, S, D = [int(i) for i in input().split()]
print(a_moving_walkway(L, X, Y, S, D))
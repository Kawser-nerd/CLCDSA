#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

#define FOR(i, m, k) for(int i = m; i < k; ++i)
#define REP(i, k) for(int i = 0; i < k; ++i)

int main() {
	int N;
	cin >> N;
	cout << N * 2 << endl;
	system("pause");
	return 0;
}
#include <stdio.h>
#include <iostream>
#include <sstream>
#include <fstream>
#include <cmath>

using namespace std;

int main(int argc, char ** argv) {

	int X;
	cin >> X;

	//cout << sqrt(sqrt(X)) << endl;
	cout << pow(X, 0.25) << endl;
}
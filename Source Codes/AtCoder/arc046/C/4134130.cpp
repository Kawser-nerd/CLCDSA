#include <iostream>
#include <cstdio>
#include <algorithm>
#include <vector>
#include <string>
#include <set>
#include <map>
#include <queue>
#include <cmath>
 
using namespace std;
 
#define rep(i,n) for(int i = 0; i<n ; i++)
#define FOR(i,a,n) for(int i=a; i<n; i++)
 
#define RANGE(a,i,b) ((a)<=(i)&&(i)<=(b))
 
#define pb push_back
#define mp make_pair
 
#define ALL(a) (a).begin(), (a).end()
 
#define SET(a,c) memset(a,c,sizeof(a))
#define CLR(a) memset(a,0,sizeof(a))
 
#define INF 1001001001
#define LLINF 1001001001001001001LL
 
typedef long long ll;
typedef pair<int,int> PII;
typedef vector<int> VI;
 
#define PQ priority_queue
 
#define FST first
#define SEC second
 
int dx[4]={1,0,-1,0};
int dy[4]={0,1,0,-1};
 
//------------------HEAD---------------------
PII men[151234]; // ??m???f
PII women[151234]; // ??m???f

// ?????or??????????????????????????
// ?????????multiset??????????????
// ?????????????????????????????????????????????
// ??????????multiset?????????????????????multiset?????????????????????
// multiset??????????????????????????????????


int main(void){
    int n,m;
    cin>>n>>m;
    rep(i,n)cin>>men[i].FST>>men[i].SEC;
    rep(i,m)cin>>women[i].SEC>>women[i].FST;
    sort(men,men+n);
    sort(women,women+m);
    int j=0; // ???index
    multiset<int> s; // ???????
    int ans=0;
    rep(i,n){
        while(j<m&&women[j].FST<=men[i].FST){
            cerr<<"f "<<j<<endl;
            s.insert(women[j].SEC);
            ++j;
            
        }
        cerr<<"m "<<i<<endl;
        auto it = s.lower_bound(men[i].SEC);
        if(it!=s.end()) {
            ++ans;
            s.erase(it);
        }
    }
    cout<<ans<<endl;
    
    return 0;
}
def a_paint(X1, Y1, R, X2, Y2, X3, Y3):
    """
    ??(X1, Y1)??????R???????????
    X2<=X<=X3, Y2<=Y<=Y3 ????????????
    ???????????????
    ???????????????????????????
    """

    # ???????????????????????????????
    is_rectangle_include_circle = all([X2 <= X1 - R, X1 + R <= X3, Y2 <= Y1 - R, Y1 + R <= Y3])

    # ???????????????????????????????
    is_circle_include_rectangle = all([(X1 - X2)**2 + (Y1 - Y2)**2 <= R**2,
                                       (X1 - X3)**2 + (Y1 - Y3)**2 <= R**2,
                                       (X1 - X2)**2 + (Y1 - Y3)**2 <= R**2,
                                       (X1 - X3)**2 + (Y1 - Y2)**2 <= R**2])

    # ??????????????????
    if is_rectangle_include_circle:
        ans = 'NO YES'
    elif is_circle_include_rectangle:
        ans = 'YES NO'
    else:
        ans = 'YES YES'
    return ans

X1, Y1, R = [int(i) for i in input().split()]
X2, Y2, X3, Y3 = [int(i) for i in input().split()]
print(a_paint(X1, Y1, R, X2, Y2, X3, Y3))
import java.util.*;
 
public class Main {
    private static Scanner scanner = new Scanner(System.in);
 
    public static void main(String[]$) {
        System.out.println(scanner.next().equalsIgnoreCase(scanner.next()) ? "Yes" : "No");
    }
}
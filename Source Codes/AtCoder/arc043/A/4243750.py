def a_transformation(N, A, B, S):
    # ?????????????E, ??????????D?????????
    # ????P????????P??????????????P?????
    # ????+Q???????+Q???????????????????
    # ???????
    # P = B / D
    # sum(P*S + Q) = N*A ? Q = A - P*sum(S)/N = A - P*E = A - (E*B)/D
    # ?????????????P, Q??????????????????????????
    # D=0???????????????????P, Q???????

    e = sum(S) / N
    d = max(S) - min(S)
    if d == 0:
        return -1
    p = B / d
    q = A - (e * B) / d
    ans = '{0:.09f} {1:.09f}'.format(p, q)
    return ans

N, A, B = [int(i) for i in input().split()]
S = [int(input()) for _ in [0] * N]
print(a_transformation(N, A, B, S))
def c_24(N, K):
    MOD = 1777777777

    def inverse(n):
        return pow(n, MOD - 2, MOD)  # mod MOD???n???

    def comb(n, r):
        numerator, denominator = 1, 1
        for j in range(n, n - r, -1):
            numerator = (numerator * j) % MOD
        for j in range(r, 0, -1):
            denominator = (denominator * j) % MOD
        return (numerator * inverse(denominator)) % MOD

    def montmort(n):
        # 1~n ????????? a ?????? a_i != i ?????????????
        # ??????????????????????
        t1, t2 = 0, 1
        for k in range(3, n + 1):
            t1, t2 = t2, ((k - 1) * (t1 + t2)) % MOD
        return t2

    # N??????????????????????comb(N, K)??
    # K???????????????????????????????????
    ans = (comb(N, K) * montmort(K)) % MOD
    return ans

N, K = [int(i) for i in input().split()]
print(c_24(N, K))
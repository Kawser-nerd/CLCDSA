'''
https://beta.atcoder.jp/contests/agc027/submissions/3201641
'''
n, m = map(int, input().split())
s = list(input())

deg = [{'A':0, 'B':0} for _ in range(n)]  # deg[0 - (n - 1)][0 - 1] = 0
g = [list() for _ in range(n)]  # g[0 - (n - 1)] = []

for i in range(m):  # ?????????
    x, y = map(int, input().split())
    x, y = x - 1, y - 1  # 1-indexed -> 0-indexed
    deg[x][s[y]] += 1  # ?? x ????????????? : A ? B ?????, ?????????? 1 ???
    deg[y][s[x]] += 1  # ?? y ????????????? : A ? B ?????, ?????????? 1 ???
    g[x].append(y)  # ?? x ???????????? y ???
    g[y].append(x)  # ?? y ???????????? x ???

was = [False for _ in range(n)]  # was[j] = True ?? ?? j ? que ?????
que = []  # A ? B ????????, ?????????????

for i in range(n):  # ???????
    if deg[i]['A'] == 0 or deg[i]['B'] == 0:  # A ? B ????????
        que.append(i)  # ??????? que ???
        was[i] = True  #  was[j] = True ?, ??????? que ?????????

for q in que:  # A ? B ????????, ???????????????? que ?????????????
    for j in g[q]:  # ???????????????
        deg[j][s[q]] -= 1  # "???????????" ????????????? : ????????????????
        if deg[j]['A'] == 0 or deg[j]['B'] == 0:  # "???????????" ? A ? B ????????
            if not was[j]:  # ?? j ? que ?????????????, que ????? / ??????, ????????????, ????
                was[j] = True
                que.append(j)  # ??????? que ???, ????????

# ????????????, ?????????, AABB... ??????
# ???????????, ???, que ?????????????, AABB...???????
print('No' if len(que) == n else 'Yes')
#define ForSubmission

using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;

namespace AtCoder
{
	// http://abc015.contest.atcoder.jp/tasks/abc015_3
	public class Abc015_3
	{
#if ForSubmission
		public static void Main(string[] args)
		{
			Abc015_3.Solve(args);
		}
#endif

		public static void Solve(string[] args)
		{
			string[] str = Console.ReadLine().Split(' ');
			var ??? = int.Parse(str[0]);
			var ??? = int.Parse(str[1]);

			var ??????? = new int[???][];

			foreach (var i in Enumerable.Range(0, ???))
			{
				???????[i] = Console.ReadLine().Split(' ').Select(s => int.Parse(s)).ToArray();
			}

			Func<List<List<int>>, int[], List<List<int>>> ???????? = (????, ??????) =>
			{
				var result = new List<List<int>>();
				for (var i = 0; i < ??????.Length; i++)
				{
					var ??????????? = ??????[i];
					for (var j = 0; j < ????.Count; j++)
					{
						var buff = new List<int>(????[j]){ ??????????? };
						result.Add(buff);
					}
				}

				return result;
			};

			var ???????? = new List<List<int>>() { new List<int>() };

			for (var i = 0; i < ???; i++)
			{
				???????? = ????????(????????, ???????[i]);
			}

			var ???????? = ????????.Select(????? => ?????.Aggregate((a,b) => a^b)).Any(num => num == 0);

			if (????????)
			{
				Console.WriteLine("Found");
				return;
			}

			Console.WriteLine("Nothing");
		}
	}
}
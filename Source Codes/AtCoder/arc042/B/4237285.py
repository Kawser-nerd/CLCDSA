def b_ant(X, Y, N, Pos):
    """
    ??????????(?????)??????????????(???????)
    ???????????????????????????????????
    """

    # ?????????????????????????????????????
    # ????????????????????
    # ??????????????????????????????????
    import numpy
    import math
    pos_initial = numpy.array([X, Y])
    ans = float('inf')
    for i in range(N):
        v1 = numpy.array(Pos[i])
        v2 = numpy.array(Pos[(i + 1) % N])

        vec_edge = v2 - v1  # ????????????????????
        vec_init = pos_initial - v1  # ??????????????????????

        vec_edge_norm = numpy.linalg.norm(vec_edge)
        vec_init_norm = numpy.linalg.norm(vec_init)
        # 2??????????
        theta = math.acos(numpy.dot(vec_edge, vec_init)
                          / (vec_edge_norm * vec_init_norm))
        # ????????????????????????
        ans = min(ans, vec_init_norm * math.sin(theta))
    ans = round(ans, 10)  # ??10e-6???????????????????????
    return ans

X, Y = [int(i) for i in input().split()]
N = int(input())
Pos = [[int(i) for i in input().split()] for j in range(N)]
print(b_ant(X, Y, N, Pos))
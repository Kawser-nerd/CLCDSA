def b_special_jury_prize(N, K, X):
    import heapq

    # priority queue???????pop????
    # ???????pop??????????????
    heap = [(-X[k], k + 1) for k in range(K)]
    heapq.heapify(heap)

    ans = [heap[0][1]]
    for j in range(K, N):
        # ?????1??????????????????????????????
        # K+1??????????????????????????????????
        heapq.heappushpop(heap, (-X[j], j + 1))
        ans.append(heap[0][1])
    ans = '\n'.join(map(str, ans))
    return ans

N, K = [int(i) for i in input().split()]
X = [int(i) for i in input().split()]
print(b_special_jury_prize(N, K, X))
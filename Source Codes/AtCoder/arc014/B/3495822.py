def b_shiritori(N, W):
    word_said = []  # ????????????
    last_word = W[0][0]  # ?????????????????????????
    ans = 'DRAW'
    for turn, word in enumerate(W):
        if last_word[-1] != word[0] or word in word_said:
            ans = 'WIN' if turn % 2 == 1 else 'LOSE'  # ??????????????
            break
        last_word = word
        word_said.append(word)
    return ans

N = int(input())
W = [input() for _ in range(N)]
print(b_shiritori(N, W))
#include <iostream>
#include <string>

int main()
{
	std::string S;
	std::cin >> S;

	if ( S.at( 0 ) == 'A' )
	{
		int counter = 0;
		int index = 0;
		for ( int i = 2; i <= S.length() - 2; i++ )
		{
			if ( S.at( i ) == 'C' )
			{
				counter++;
				index = i;	//	C?????????????
			}
		}

		if ( counter == 1 )
		{
			// ???C???????????????????????
			for ( int i = 1; i < S.length(); i++ )
			{
				if ( i != index )
				{
					char tmp = S.at( i );

					if ( tmp >= 'a' && tmp <= 'z' )
					{
						// OK
					}
					else
					{
						// ??????????????NG
						std::cout << "WA" << std::endl;
						return ( 0 );
					}
				}
			}

			// for????????OK????AC???
			std::cout << "AC" << std::endl;
		}
		else
		{
			// C?2??????????????????
			std::cout << "WA" << std::endl;
		}
	}
	else
	{
		// ???A???????
		std::cout << "WA" << std::endl;
	}

	return ( 0 );
}
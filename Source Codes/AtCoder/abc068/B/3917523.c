#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i,j,n,o,po,s;
    int shuzu[100860];
    while(~scanf("%d",&n))
    {
        for(i=1; i<=n; i*=2)
        {
            if(i*2>n)
            {
                break;
            }

        }
        printf("%d\n",i);
    }
    return 0;
}
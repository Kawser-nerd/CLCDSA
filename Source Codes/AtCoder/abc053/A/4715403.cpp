#include <iostream>
using namespace std;

int main()
{
    cin.tie(0);
    ios::sync_with_stdio(false);

    int x;
    cin >> x;
    x < 1200 ? cout << "ABC" : cout << "ARC";
    cout << endl;

    return 0;
}
/* 
 * ???AtCoDeer??????????????????????? AtCoDeer????????????? 0 ? ? (0,0) ????? 1 ?? N ???? i ?????? ti ? ? (xi,yi) ?????????
 * 
 * AtCoDeer????? t ? ? (x,y) ????? ?? t+1 ?? ? (x+1,y), (x?1,y), (x,y+1), (x,y?1) ???????????????????? ???????????????????????????
 * AtCoDeer??????????????????????????
 */

import java.util.*;
	public class Main{
        public static void main(String[] args){
            Scanner sc = new Scanner(System.in);
            //??
            int n = Integer.parseInt(sc.next());
            
            int t = 0;
            int ti = 0;
            int x = 0;
            int xi = 0;
            int y = 0;
            int yi = 0;
            // ??????????d
            int d = 0;
            
            // ???????????????
            for(int i=0; i<n; i++){
            	ti = Integer.parseInt(sc.next());
            	xi = Integer.parseInt(sc.next());
            	yi = Integer.parseInt(sc.next());
            	
            	d = (int)(Math.abs(xi-x) + Math.abs(yi-y));
            	
            	// d??????????d????????(????????????)???No?????return
            	if(ti-t < d || Math.abs(d-ti+t)%2 == 1){
            		System.out.println("No");
                    sc.close();
                    return;
            	}
            	t = ti;
            	x = xi;
            	y = yi;
            }
            
            // ?????????????Yes???
            System.out.println("Yes");
            sc.close();
        }
    }
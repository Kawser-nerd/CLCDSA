using System;

namespace AtCoderConsole
{
    class Program
    {
        static void Main(string[] args)
        {
            // ??????1??????
            string input = Console.ReadLine();

            // ?????????
            int n = int.Parse(input);

            // ????????????????
            int[] takoyaki = new int[n];
            
            // ?????????????
            for(int i = 0; i < takoyaki.Length; i++)
            {
                takoyaki[i] = int.Parse(Console.ReadLine());
            }

            // ????????
            Array.Sort(takoyaki);

            // ??????????(???????????????)?????
            Console.WriteLine(takoyaki[0]);
        }
    }
}
def b_books(A, B, N, Boxes):
    import math

    # ????????????
    a, b = min(A, B), max(A, B)
    # a, b????????????????
    hypotenuse = math.sqrt(a**2 + b**2)
    theta = math.atan(a / b)

    ans = []
    for c, d in Boxes:
        c, d = min(c, d), max(c, d)

        if c >= a and d >= b:
            ans.append('YES')  # ??????????90???????????
        else:
            # 0?(??????????)??atan(b/a)?(??????????)???
            # ????????????????????
            upper, lower = math.atan(b / a), 0
            # ?1?????????????????(??????5000)???????
            for _ in range(50):
                mid = (upper + lower) / 2
                # ???????mid??????????????????????
                # ????????????????????????????
                if hypotenuse * math.sin(mid + theta) <= d:
                    lower = mid
                else:
                    upper = mid
            # ???????????? ??lower??????????????
            # ???????????????????????????????????
            if hypotenuse * math.cos(lower - theta) <= c:
                ans.append('YES')
            else:
                ans.append('NO')
    ans = '\n'.join(map(str, ans))
    return ans

A, B = [int(i) for i in input().split()]
N = int(input())
Boxes = [[int(i) for i in input().split()] for j in range(N)]
print(b_books(A, B, N, Boxes))
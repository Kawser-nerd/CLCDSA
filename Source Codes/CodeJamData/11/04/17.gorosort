#include<stdio.h>
int main()
{
	 int t,cse=0,n,x,i;
	 float ans;
	 scanf("%d",&t);
	 while(t--) {
	 	cse++;
	 	ans=0;
	 	scanf("%d",&n);
	 	for(i=1;i<=n;i++) {
	 		scanf("%d",&x);
	 		if(x!=i) ans++;
	 		}
	 	printf("Case #%d: %f\n",cse,ans);
	 	}
	 return 0;
}

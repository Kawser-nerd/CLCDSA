def b_name(A):
    # ???A??????????????????
    # e.g. ABBBAA => 1, ABABC => 1(3??????)
    count_diff = 0
    for k in range(len(A) // 2):
        if A[k] != A[len(A) - k - 1]:
            count_diff += 1

    ans = len(A) * 25  # ???????????????????????????
    if count_diff == 0:
        # ???A???????
        # ??????????????????
        # |A|?????????????????????????25?????
        ans -= (len(A) % 2) * 25
    elif count_diff == 1:
        # 1????????????????????????????
        # ?????????????????????????????????
        # ????24????????????????2?????
        ans -= 2
    else:
        # 2??????????????????????????????
        # ????????????????
        pass
    return ans

A = input()
print(b_name(A))
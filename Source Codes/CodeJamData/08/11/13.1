#!/usr/bin/python

import sys

def opp_cmp(elt1, elt2):
        return cmp(elt2, elt1)

inputfile = open(sys.argv[1], 'r').read().split('\n')
outputfile = open("./output.1", 'w')

numcases = int(inputfile[0])
totallines = 1
currcase = 0

while currcase < numcases:
        n = int(inputfile[totallines])
        totallines += 1
        line1 = inputfile[totallines]
        totallines += 1
        line2 = inputfile[totallines]
        totallines += 1

        vec1 = line1.split(' ')
        vec2 = line2.split(' ')

        i = 0
        while i < n:
                vec1[i] = int(vec1[i])
                vec2[i] = int(vec2[i])
                i += 1

        vec1.sort()
        vec2.sort(opp_cmp)

        i = 0
        minprod = 0
        while i < n:
                minprod += vec1[i]*vec2[i]
                i += 1

        currcase += 1
        outputfile.write("Case #" + str(currcase) + ": " + str(minprod) + '\n')
using System;

namespace AtCoderConsole
{
    class Program
    {
        static void Main(string[] args)
        {
            // ??????1????
            string input = Console.ReadLine();

            // ???????????????
            int price = int.Parse(input.Split(' ')[0]);

            // ??????????????????????
            int num = int.Parse(input.Split(' ')[1]);
            int[] ngNums = new int[num];

            // ???????????????
            input = Console.ReadLine();

            // ????????
            for(int i = 0; i < num; i++)
            {
                ngNums[i] = int.Parse(input.Split(' ')[i]);
            }

            // ???????????????????
            while (true)
            {
                // ????????????????????break
                if (check(price, ngNums))
                {
                    break;
                }

                // price??????????
                price++;
            }

            // ???????
            Console.WriteLine(price);
        }

        // ????????????????????
        static bool check(int price, int[] ng)
        {
            // price??????????
            string priceStr = price.ToString();

            // ??????????????false???
            for (int i = 0; i < ng.Length; i++)
            {
                if (priceStr.Contains(ng[i].ToString()))
                {
                    return false;
                }
            }
            // ?????????true???
            return true;
        }
    }
}